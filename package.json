{
  "name": "peerjs-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "PORT=9000 npm start",
    "start": "npx peerjs --port $PORT --key peerjs --path /",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "license": "ISC",
  "author": "fernandopascoalbr",
  "dependencies": {
    "peer": "^1.0.0"
  }
}